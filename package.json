{
  "name": "memedle",
  "version": "1.0.0",
  "private": true,
  "description": "Memedle — a daily memecoin guessing game. A static page plus five serverless functions for handles, wallets and the board.",
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@vercel/blob": "^2.8.0"
  },
  "scripts": {
    "test": "node tools/run-test.mjs",
    "stamp": "node tools/bump-assets.js",
    "validate": "node tools/validate-data.js",
    "scene": "node tools/build-scene.mjs",
    "shot": "node tools/shot.mjs"
  }
}
